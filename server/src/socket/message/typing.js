export function typing({io, socket}) {
    return async (payload) => {
        /**
         * Validate user presence
         */
        /**
         * Emit the typing status to the room
         */
    }
}